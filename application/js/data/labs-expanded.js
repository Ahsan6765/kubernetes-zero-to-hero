const step = (title, detail, command, output) => ({ title, detail, command, output });
const lab = (id, icon, title, description, difficulty, duration, steps) => ({ id, icon, title, description, difficulty, duration, steps });

export const labs = [
    lab("deploy-inspect-pod", "☸", "Deploy and Inspect a Kubernetes Pod", "Deploy an NGINX Pod, inspect runtime details, read logs, and practice the Pod lifecycle.", "Beginner", "15 min", [
        step("Check cluster connectivity", "Confirm kubectl can reach a cluster and that a node is Ready.", "kubectl get nodes", "minikube   Ready   control-plane   v1.31.0"),
        step("Create an NGINX Pod", "kubectl run creates a standalone Pod for a quick experiment.", "kubectl run nginx --image=nginx --port=80", "pod/nginx created"),
        step("Inspect status and placement", "The wide output shows the Pod IP and selected node.", "kubectl get pod nginx -o wide", "nginx   1/1   Running   10.244.0.18   minikube"),
        step("Inspect, execute, and clean up", "Use describe, logs, and exec, then remove the Pod.", "kubectl describe pod nginx && kubectl logs nginx && kubectl exec nginx -- nginx -v && kubectl delete pod nginx", "Status: Running\nnginx version: nginx/1.27.0\npod \"nginx\" deleted")
    ]),
    lab("pod-yaml-manifest", "📄", "Create a Kubernetes Pod Using a YAML Manifest", "Write a declarative Pod definition and learn how resource fields describe desired state.", "Beginner", "20 min", [
        step("Create the manifest", "Save this as pod.yaml. apiVersion, kind, metadata, and spec describe the resource.", "cat > pod.yaml <<'EOF'\napiVersion: v1\nkind: Pod\nmetadata:\n  name: manifest-nginx\nspec:\n  containers:\n  - name: nginx\n    image: nginx:1.27\nEOF", "The file pod.yaml now contains a Pod manifest."),
        step("Discover the schema", "kubectl explain lets you inspect fields without memorizing every YAML property.", "kubectl explain pod.spec.containers", "KIND: Pod\nVERSION: v1\nFIELD: containers <[]Object>"),
        step("Apply and inspect", "apply creates the resource; YAML output includes live status fields.", "kubectl apply -f pod.yaml && kubectl get pod manifest-nginx -o yaml", "pod/manifest-nginx created\nstatus:\n  phase: Running"),
        step("Delete from the manifest", "Keep cleanup tied to the definition you applied.", "kubectl delete -f pod.yaml", "pod \"manifest-nginx\" deleted")
    ]),
    lab("deployment-lifecycle", "🚀", "Deploy, Scale, Upgrade, and Roll Back an Application", "Manage a replicated application through Deployments, ReplicaSets, rolling updates, and rollback.", "Beginner", "25 min", [
        step("Create and inspect a Deployment", "A Deployment manages replicas through a ReplicaSet, which owns the Pods.", "kubectl create deployment web --image=nginx:1.24 --replicas=3 && kubectl get deployment,replicaset,pods", "deployment.apps/web created\ndeployment.apps/web  3/3  3  3\nreplicaset.apps/web  3  3  3"),
        step("Scale the application", "Changing replicas increases or decreases the desired number of Pods.", "kubectl scale deployment web --replicas=5 && kubectl rollout status deployment/web", "deployment.apps/web scaled\ndeployment successfully rolled out"),
        step("Perform a rolling update", "Changing the image creates a new ReplicaSet and replaces Pods gradually.", "kubectl set image deployment/web nginx=nginx:1.25 && kubectl rollout status deployment/web", "deployment.apps/web image updated\ndeployment successfully rolled out"),
        step("Review and undo the revision", "Use history to inspect revisions, then undo when the new version must be reverted.", "kubectl rollout history deployment/web && kubectl rollout undo deployment/web", "REVISION  CHANGE-CAUSE\n1         <none>\n2         <none>\ndeployment.apps/web rolled back")
    ]),
    lab("service-exposure", "🌐", "Expose an Application Using a Kubernetes Service", "Give Pods a stable ClusterIP endpoint and trace traffic through selectors and endpoints.", "Intermediate", "20 min", [
        step("Create an application", "A Deployment gives the Service replicated, labeled Pods to route to.", "kubectl create deployment web --image=nginx --replicas=3 && kubectl rollout status deployment/web", "deployment.apps/web created\ndeployment successfully rolled out"),
        step("Create a ClusterIP Service", "The selector matches Pod labels and targetPort forwards to port 80.", "kubectl expose deployment web --name=web --port=80 --target-port=80", "service/web exposed"),
        step("Inspect the virtual endpoint", "The ClusterIP is stable even when individual Pod IPs change.", "kubectl get service web && kubectl get endpoints web", "web   ClusterIP   10.96.12.40   80/TCP\nweb   10.244.0.18:80,10.244.0.19:80"),
        step("Test traffic and clean up", "Run a temporary diagnostic Pod, then remove the resources.", "kubectl run curl --rm -i --restart=Never --image=curlimages/curl -- curl -s http://web && kubectl delete service web deployment web", "<h1>Welcome to nginx!</h1>\npod \"curl\" deleted\nservice and deployment deleted")
    ]),
    lab("labels-selectors", "🏷", "Manage Applications Using Labels and Selectors", "Organize resources with labels and use selectors to query Pods and control Service membership.", "Beginner", "20 min", [
        step("Create labeled workloads", "Labels are key-value metadata used for grouping and selection.", "kubectl create deployment web --image=nginx --replicas=2 && kubectl label deployment web app=web environment=dev", "deployment.apps/web created\ndeployment.apps/web labeled"),
        step("View and filter labels", "show-labels displays labels; selectors return only matching resources.", "kubectl get pods --show-labels && kubectl get pods -l app=web,environment=dev", "web-6b8d7  1/1  Running  app=web,environment=dev"),
        step("Change a label", "Avoid changing labels that a Service uses unless you intend to remove traffic endpoints.", "kubectl label deployment web team=platform && kubectl get deployment -l team=platform", "deployment.apps/web labeled\nweb  2/2  2"),
        step("Remove the label and clean up", "A trailing hyphen removes a label key.", "kubectl label deployment web team- && kubectl delete deployment web", "deployment.apps/web unlabeled\ndeployment.apps \"web\" deleted")
    ]),
    lab("configmaps", "⚙", "Configure Applications Using ConfigMaps", "Separate environment-specific configuration from container images with a ConfigMap.", "Beginner", "20 min", [
        step("Create configuration data", "ConfigMaps are for non-sensitive values such as feature flags and application modes.", "kubectl create configmap app-config --from-literal=APP_MODE=development --from-literal=LOG_LEVEL=info", "configmap/app-config created"),
        step("Create a consumer workload", "envFrom imports every ConfigMap key as an environment variable.", "kubectl create deployment config-demo --image=busybox -- /bin/sh -c 'sleep 3600' && kubectl set env deployment/config-demo --from=configmap/app-config", "deployment.apps/config-demo created\ndeployment.apps/config-demo env updated"),
        step("Inspect and verify", "describe shows keys and values; exec confirms the application received them.", "kubectl describe configmap app-config && kubectl exec deployment/config-demo -- printenv APP_MODE", "APP_MODE: development\nLOG_LEVEL: info\ndevelopment"),
        step("Clean up", "Remove the demo resources after the experiment.", "kubectl delete deployment config-demo configmap app-config", "deployment.apps \"config-demo\" deleted\nconfigmap \"app-config\" deleted")
    ]),
    lab("secrets", "🔐", "Manage Sensitive Configuration Using Kubernetes Secrets", "Store demo credentials as Secret data, inject them into a workload, and verify least exposure.", "Intermediate", "20 min", [
        step("Create a demo Secret", "Use throwaway values only. Base64 encoding is not encryption.", "kubectl create secret generic app-secret --from-literal=USERNAME=demo --from-literal=PASSWORD=not-for-production", "secret/app-secret created"),
        step("Create a Secret consumer", "Import Secret keys as environment variables; never use real credentials in this lab.", "kubectl create deployment secret-demo --image=busybox -- /bin/sh -c 'sleep 3600' && kubectl set env deployment/secret-demo --from=secret/app-secret", "deployment.apps/secret-demo created\ndeployment.apps/secret-demo env updated"),
        step("Inspect metadata safely", "describe shows key names without printing Secret values. Avoid sharing -o yaml output.", "kubectl describe secret app-secret", "Name: app-secret\nType: Opaque\nData: PASSWORD 16 bytes, USERNAME 4 bytes"),
        step("Verify access and clean up", "Confirm the intended container received the value, then delete the demo resources.", "kubectl exec deployment/secret-demo -- printenv USERNAME && kubectl delete deployment secret-demo secret app-secret", "demo\ndeployment.apps \"secret-demo\" deleted\nsecret \"app-secret\" deleted")
    ]),
    lab("namespaces", "🗂", "Isolate Workloads Using Kubernetes Namespaces", "Separate environments with namespaces and practice namespace-scoped resource queries.", "Beginner", "20 min", [
        step("Create an environment namespace", "Namespaces provide a logical boundary for most namespaced resources.", "kubectl create namespace lab-dev", "namespace/lab-dev created"),
        step("Deploy into the namespace", "The -n flag directs the command to lab-dev instead of the current namespace.", "kubectl -n lab-dev create deployment web --image=nginx", "deployment.apps/web created"),
        step("Query one or all namespaces", "Use --all-namespaces when investigating workloads outside your current context.", "kubectl get pods -n lab-dev && kubectl get pods --all-namespaces", "lab-dev  web-6b8d7  1/1  Running\n..."),
        step("Restore context and clean up", "Changing the current namespace persists in kubeconfig, so restore it after the exercise.", "kubectl config set-context --current --namespace=default && kubectl delete namespace lab-dev", "Context modified.\nnamespace \"lab-dev\" deleted")
    ]),
    lab("resource-requests-limits", "📊", "Configure Kubernetes Resource Requests and Limits", "Declare CPU and memory expectations so the scheduler and runtime can manage workload resources.", "Intermediate", "20 min", [
        step("Create a resource-managed workload", "Requests influence scheduling; limits cap usage. Neither guarantees performance by itself.", "kubectl create deployment resource-demo --image=nginx --replicas=2 && kubectl set resources deployment/resource-demo --requests=cpu=100m,memory=128Mi --limits=cpu=500m,memory=256Mi", "deployment.apps/resource-demo created\nresource requirements updated"),
        step("Wait for the rollout", "The changed Pod template causes the Deployment to replace its Pods.", "kubectl rollout status deployment/resource-demo", "deployment successfully rolled out"),
        step("Inspect the declared resources", "Look under the container resources field in the Deployment spec.", "kubectl get deployment resource-demo -o jsonpath='{.spec.template.spec.containers[0].resources}'", "limits: cpu 500m, memory 256Mi\nrequests: cpu 100m, memory 128Mi"),
        step("Verify and clean up", "describe shows values Kubernetes evaluates on the running Pod.", "kubectl describe pod -l app=resource-demo && kubectl delete deployment resource-demo", "Requests: cpu 100m, memory 128Mi\nLimits: cpu 500m, memory 256Mi\ndeployment deleted")
    ]),
    lab("node-selectors", "🎯", "Control Pod Placement Using Node Selectors", "Label a node and constrain a Pod to nodes matching a hardware or environment label.", "Intermediate", "20 min", [
        step("Inspect and label a node", "Choose a disposable lab node and do not overwrite labels used by production policies.", "kubectl get nodes --show-labels && kubectl label node minikube hardware=lab", "minikube  Ready\nnode/minikube labeled"),
        step("Schedule with nodeSelector", "The Pod can only run on a node labeled hardware=lab.", `kubectl run selector-demo --image=nginx --overrides='{"spec":{"nodeSelector":{"hardware":"lab"}}}'`, "pod/selector-demo created"),
        step("Verify placement", "A nonexistent label would leave a Pod Pending with an unschedulable event.", "kubectl get pod selector-demo -o wide", "selector-demo  1/1  Running  minikube"),
        step("Remove the experiment", "Remove the Pod and label so the node returns to its original state.", "kubectl delete pod selector-demo && kubectl label node minikube hardware-", "pod deleted\nnode/minikube unlabeled")
    ]),
    lab("taints-tolerations", "🚧", "Control Scheduling Using Taints and Tolerations", "Reserve a node with a taint, observe rejection, then allow a matching workload with a toleration.", "Advanced", "25 min", [
        step("Taint a disposable node", "Only do this on a node reserved for the exercise. NoSchedule rejects new non-tolerating Pods.", "kubectl taint node minikube dedicated=lab:NoSchedule", "node/minikube tainted"),
        step("Observe a rejected Pod", "Without a matching toleration, a Pod may remain Pending on a single-node cluster.", "kubectl run no-toleration --image=nginx && kubectl get pod no-toleration", "pod/no-toleration created\nno-toleration  0/1  Pending"),
        step("Add a matching toleration", "A toleration permits the taint but does not select the node; combine it with nodeSelector when needed.", `kubectl run tolerated --image=nginx --overrides='{"spec":{"tolerations":[{"key":"dedicated","operator":"Equal","value":"lab","effect":"NoSchedule"}]}}'`, "pod/tolerated created"),
        step("Inspect and clean up", "describe explains FailedScheduling. Always remove temporary taints from shared clusters.", "kubectl describe pod no-toleration && kubectl get pod tolerated -o wide && kubectl delete pod no-toleration tolerated && kubectl taint node minikube dedicated=lab:NoSchedule-", "Warning FailedScheduling: untolerated taint\ntolerated 1/1 Running minikube\nnode/minikube untainted")
    ]),
    lab("broken-deployment", "🩺", "Troubleshoot a Broken Kubernetes Deployment", "Use status, events, logs, and rollout checks to repair a deliberately broken workload.", "Advanced", "25 min", [
        step("Apply the broken workload", "Save the exercise manifest as broken-deployment.yaml first. This version has a bad image.", "kubectl apply -f broken-deployment.yaml", "deployment.apps/broken-web created"),
        step("Start at the controller", "Compare desired replicas, available replicas, ReplicaSets, and Pod status.", "kubectl get deployment,replicaset,pods", "deployment.apps/broken-web  0/2  2  0\nreplicaset.apps/broken-web  2  2  0"),
        step("Read events and logs", "describe often reveals image pull failures; logs may be unavailable when a container never starts.", "kubectl describe pod -l app=broken-web && kubectl logs -l app=broken-web", "Warning Failed: Failed to pull image nginx:not-a-real-tag: not found\ncontainer is waiting to start"),
        step("Correct, verify, and clean up", "Fix the root cause in the Deployment, then verify rollout health.", "kubectl set image deployment/broken-web nginx=nginx:1.27 && kubectl rollout status deployment/broken-web && kubectl delete deployment broken-web", "image updated\ndeployment successfully rolled out\ndeployment deleted")
    ]),
    lab("service-networking", "🔎", "Troubleshoot Kubernetes Service Networking", "Trace an internal connectivity failure from Service selectors to endpoints, DNS, and traffic.", "Advanced", "25 min", [
        step("Deploy a backend", "The backend Pods use app=backend. This label is the contract the Service must select.", "kubectl create deployment backend --image=nginx --replicas=2 && kubectl rollout status deployment/backend", "deployment.apps/backend created\ndeployment successfully rolled out"),
        step("Create a broken Service", "The Service exists, but its selector intentionally does not match the backend Pods.", `kubectl expose deployment backend --name=backend --port=80 --target-port=80 && kubectl patch service backend -p '{"spec":{"selector":{"app":"wrong-label"}}}'`, "service/backend exposed\nservice/backend patched"),
        step("Check endpoints and DNS", "An empty endpoint list proves selector and Pod labels disagree. DNS can still resolve the Service.", "kubectl get service backend && kubectl get endpoints backend && kubectl run netcheck --rm -i --restart=Never --image=busybox:1.36 -- nslookup backend", "backend 10.96.22.10\nENDPOINTS <none>\nAddress: 10.96.22.10"),
        step("Fix the selector and clean up", "Restore app=backend, confirm endpoints, then remove the demo resources.", `kubectl patch service backend -p '{"spec":{"selector":{"app":"backend"}}}' && kubectl get endpoints backend && kubectl delete service backend deployment backend`, "service patched\n10.244.0.22:80,10.244.0.23:80\nresources deleted")
    ]),
    lab("persistent-storage", "💾", "Configure Persistent Storage Using PersistentVolumes and PersistentVolumeClaims", "Claim storage, mount it into a Pod, and verify data survives Pod recreation.", "Advanced", "30 min", [
        step("Inspect available storage", "Storage differs between local and cloud clusters. Check StorageClasses first.", "kubectl get storageclass", "standard (default)  k8s.io/minikube-hostpath  Delete"),
        step("Create and bind a claim", "A PVC asks for capacity and an access mode. Dynamic provisioning creates a matching PV when supported.", "kubectl apply -f pvc.yaml && kubectl get pvc", "persistentvolumeclaim/lab-data created\nlab-data  Bound  pvc-8d3e...  1Gi  RWO"),
        step("Mount and write data", "Use a cluster-appropriate Pod manifest. hostPath examples are node-local and for labs only.", "kubectl apply -f storage-pod.yaml && kubectl exec storage-demo -- sh -c 'echo persisted > /data/marker.txt'", "pod/storage-demo created\nmarker written"),
        step("Recreate, verify, and clean up", "Recreate the Pod with the same PVC, confirm the marker, then remove lab resources.", "kubectl delete pod storage-demo && kubectl apply -f storage-pod.yaml && kubectl exec storage-demo -- cat /data/marker.txt && kubectl delete -f storage-pod.yaml pvc.yaml", "pod recreated\npersisted\npersistentvolumeclaim deleted")
    ]),
    lab("rbac-application-access", "🛡", "Configure Kubernetes RBAC for Application Access", "Grant a ServiceAccount read-only Pod access in one namespace and verify allowed and denied actions.", "Advanced", "25 min", [
        step("Create an isolated identity", "Use a dedicated namespace and ServiceAccount so existing permissions are unaffected.", "kubectl create namespace rbac-lab && kubectl -n rbac-lab create serviceaccount developer", "namespace/rbac-lab created\nserviceaccount/developer created"),
        step("Define least-privilege permissions", "A namespace-scoped Role can read Pods but cannot create, update, or delete them.", "kubectl apply -f pod-reader-role.yaml", "role.rbac.authorization.k8s.io/pod-reader created"),
        step("Bind and verify allowed access", "A RoleBinding connects the Role to the ServiceAccount in the same namespace.", "kubectl apply -f developer-rolebinding.yaml && kubectl auth can-i list pods -n rbac-lab --as=system:serviceaccount:rbac-lab:developer", "rolebinding created\nyes"),
        step("Verify denied access and clean up", "The same identity must not be able to delete Pods.", "kubectl auth can-i delete pods -n rbac-lab --as=system:serviceaccount:rbac-lab:developer && kubectl delete namespace rbac-lab", "no\nnamespace \"rbac-lab\" deleted")
    ])
];
